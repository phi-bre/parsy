import component from 'asd';

template {
    div {
        div if (condition) {
            
        }

        p for (index in computed) {
            prop: 'Hello World'
            test: 'asd'
        }

        input model (prop) {
            type: 'text'
        }

        if (asd) { // Includes a <template>

        }

        html {{
            <a>asdasd</a>
        }}

        @submit:prevent: submit;

        div if (asdsad) {
            #the-fancy-div
            .tw-text-lg
            .md:tw-text-xl
            .tw-bg-red-500
            .hover:tw-bg-blue-500
            some-props: asd
            'some:type': asd

            div { slot }
        }

        router-link {
            to: 'href'

            h4 'Hello World'

        }
    }
}

script (props: string) {
    public message: string;

    mounted() {

    }
}
//
export default class {
    public message: string;

    mounted() {

    }
}

style {

}

