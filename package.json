{
  "name": "parsy",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "lint": "tslint --fix -c tslint.json 'src/**/*.ts' 'test/**/*.ts'"
  },
  "devDependencies": {
    "@types/jest": "^24.0.23",
    "husky": "^4.2.3",
    "jest": "^24.9.0",
    "ts-jest": "^24.2.0",
    "tslint": "^6.1.0",
    "typescript": "^3.7.2"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn lint"
    }
  }
}
